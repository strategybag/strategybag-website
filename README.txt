STRATEGY BAG — LATEST FILES CORRECTED
====================================

This package was built directly from the latest uploaded:
STRATEGY-BAG-MINIMAL-FINAL-PATCH(2).zip

What was wrong:
The current styles.css contained multiple stacked generations of hero,
Heineken, About and impact-callout overrides.

What this corrects:
- removes the accumulated duplicate override blocks
- preserves the approved rocket size
- keeps the hero slogan fully visible
- cleans the white-to-blue hero background
- preserves balanced hero copy with no orphan word on desktop
- keeps Heineken as green text in Selected Experience
- preserves corrected About headline sizing
- preserves the two-line impact callout:
  STRATEGY CREATES VALUE.
  EXECUTION DELIVERS IT.

FILES TO REPLACE
----------------
Replace BOTH:
1. index.html
2. styles.css

The index.html is the latest uploaded version and is included unchanged so
the HTML and cleaned CSS remain a matched pair.

Do not change image/logo files.
